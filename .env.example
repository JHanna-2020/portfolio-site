# Only needed for GitHub Pages *project* sites (served from /<repo-name>/).
# The GitHub Actions workflow sets this for you automatically.
# Leave unset for Vercel or custom-domain deploys (defaults to "/").
# VITE_BASE=/Portfolio/
